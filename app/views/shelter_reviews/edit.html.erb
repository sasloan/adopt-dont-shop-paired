<%= render partial: 'form', locals: {
                                      path: "/shelters/#{@review.shelter.id}/reviews/#{@review.id}",
                                      method: :patch,
                                      button_text: 'Update Review'
                                    } %>

<h2> Shelters </h2>

<p><%= link_to 'New Shelter', "/shelters/:shelter_id/new" %></p>

<% @shelters.each do |shelter| %>
	<%= shelter.name %>
	<%= shelter.address %>
	<%= shelter.city %>
	<%= shelter.state %>
	<%= shelter.zip %>
<% end %>

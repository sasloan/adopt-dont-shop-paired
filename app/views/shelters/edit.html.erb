<%= render partial: 'form', locals: {
                                      path: "/shelters/#{@shelter.id}",
                                      method: :patch,
                                      button_text: 'Update Shelter'
                                    } %>

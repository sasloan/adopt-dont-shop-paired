<h2><%= @shelter.name %></h2>

<p><%= link_to "Update Shelter", "/shelters/#{@shelter.id}/edit" %></p>
<p><%= link_to "Delete Shelter", "/shelters/#{@shelter.id}", method: :delete %></p>

	<%= @shelter.address %>
	<%= @shelter.city %>
	<%= @shelter.state %>
	<%= @shelter.zip %>

<p><%= link_to "Shelter Pets", "/shelters/#{@shelter.id}/pets" %>
